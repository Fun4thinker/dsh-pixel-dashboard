# 依赖与构建产物
node_modules/
dist/

# 本机运行态文件：绝不入库
#   usage-ledger.jsonl  用量账本（含本机全部会话的用量历史）
#   balance-prefs.json  余额查询开关
#   dsh-web.log         本机启动日志
usage-ledger.jsonl
balance-prefs.json
dsh-web.log
*.log

# 打包/校验过程的临时产物
#   打包脚本在语法错误时会落一份坏产物到 lib/client.js.invalid（仓库根就是发布包）。
#   早先这里写的是 packages/plugin/lib/...，那是两包形态的旧路径，早已不存在——
#   路径写错本身不报错，只会让坏产物**真的被提交进去**，所以按后缀忽略。
*.tgz
*.tmp
*.invalid

# 编辑器与系统
.vscode/
.idea/
.DS_Store
Thumbs.db

# 开发笔记：踩坑记录、架构取舍、闸门意图。面向改代码的人（含 AI 助手），
# 不是面向用户的说明（那是 README.md 的职责），因此不入库。
AGENT.md

# 本机开发用的 agent skill（DSH 的项目级 skill 根）。
# 当前装的是 readme-author：写 / 重构 README 时用。它是开发者的本机工具，
# 与插件功能无关，因此不入库——想让它跟着仓库走，删掉这一行即可。
.dsh/
